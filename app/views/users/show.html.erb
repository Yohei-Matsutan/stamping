<h1>管理者用User記録確認画面</h1>
<p>打刻記録一覧</p>

<h2><%= @user.name %></h2>

<%= flash[:edited] %>
<%= flash[:deleted] %>
<%= render 'stamp_table', stamps: @stamps %>

<%= link_to "CSV出力",user_path(@user.id, format: :csv) %>